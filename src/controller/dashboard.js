const { default: Movie } = require("../../client/components/public/movie/Movie")

const models = require('../models/models');
const Movies = models.moviemodel;

const getDashInfo = (req, res) => {
    Movies.
}

module.exports = {
    getDashInfo
}